# 🚀 Cloudflare Pages Configuration
# Este archivo es OPCIONAL para Cloudflare Pages
# Las variables de entorno se configuran en Cloudflare Dashboard, no aquí

name = "mi-entradas"

[build]
command = "npm run build"
cwd = "."
watch_paths = [
  "src/**/*.jsx",
  "src/**/*.css",
  "index.html",
  "public/**/*"
]
